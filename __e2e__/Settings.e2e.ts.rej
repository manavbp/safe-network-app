diff a/__e2e__/Settings.e2e.ts b/__e2e__/Settings.e2e.ts	(rejected hunks)
@@ -7,3 +7,2 @@ import { getPageUrl, getPageTitle } from './helpers';
-const navigateToSettingsPage = async ( t ) =>  (
-    t.click( Selector( 'button' ).withAttribute( 'aria-label', 'Settings' ) )
-)
+const navigateToSettingsPage = async ( t ) =>
+    t.click( Selector( 'button' ).withAttribute( 'aria-label', 'Settings' ) );
@@ -11,6 +10,4 @@ const navigateToSettingsPage = async ( t ) =>  (
-fixture`Settings Page`
-    .page( '../app/app.html' )
-    .beforeEach( async () => {
-        await waitForReact();
-    } )
-    // .afterEach( assertNoConsoleErrors );
+fixture`Settings Page`.page( '../app/app.html' ).beforeEach( async () => {
+    await waitForReact();
+} );
+// .afterEach( assertNoConsoleErrors );
@@ -23,5 +20,3 @@ test( 'can navigate to settings page', async ( t ) => {
-    navigateToSettingsPage( t )
-    await t
-        .expect( getPageUrl() )
-        .contains( '#/settings' )
-} )
+    navigateToSettingsPage( t );
+    await t.expect( getPageUrl() ).contains( '#/settings' );
+} );
@@ -30 +25 @@ test( 'can toggle switch buttons', async ( t ) => {
-    navigateToSettingsPage( t )
+    navigateToSettingsPage( t );
@@ -32,2 +27,5 @@ test( 'can toggle switch buttons', async ( t ) => {
-    const Preferences = Selector( 'ul' ).withAttribute( 'aria-label', 'Preferences' )
-    const PreferencesItemArray = Preferences.child( 'li' )
+    const Preferences = Selector( 'ul' ).withAttribute(
+        'aria-label',
+        'Preferences'
+    );
+    const PreferencesItemArray = Preferences.child( 'li' );
@@ -35 +33 @@ test( 'can toggle switch buttons', async ( t ) => {
-    const AutoUpdatePreference = PreferencesItemArray.nth( 0 )
+    const AutoUpdatePreference = PreferencesItemArray.nth( 0 );
@@ -38 +36,3 @@ test( 'can toggle switch buttons', async ( t ) => {
-        .expect( AutoUpdatePreference.find( '.MuiListItemText-primary' ).textContent )
+        .expect(
+            AutoUpdatePreference.find( '.MuiListItemText-primary' ).textContent
+        )
@@ -42,2 +42,2 @@ test( 'can toggle switch buttons', async ( t ) => {
-        .ok()
-} )
+        .ok();
+} );
@@ -46 +46 @@ test( 'can toggle back switch button from on state', async ( t ) => {
-    navigateToSettingsPage( t )
+    navigateToSettingsPage( t );
@@ -48,2 +48,5 @@ test( 'can toggle back switch button from on state', async ( t ) => {
-    const Preferences = Selector( 'ul' ).withAttribute( 'aria-label', 'Preferences' )
-    const PreferencesItemArray = Preferences.child( 'li' )
+    const Preferences = Selector( 'ul' ).withAttribute(
+        'aria-label',
+        'Preferences'
+    );
+    const PreferencesItemArray = Preferences.child( 'li' );
@@ -51 +54 @@ test( 'can toggle back switch button from on state', async ( t ) => {
-    const LaunchOnStartPreference = PreferencesItemArray.nth( 2 )
+    const LaunchOnStartPreference = PreferencesItemArray.nth( 2 );
@@ -54 +57,3 @@ test( 'can toggle back switch button from on state', async ( t ) => {
-        .expect( LaunchOnStartPreference.find( '.MuiListItemText-primary' ).textContent )
+        .expect(
+            LaunchOnStartPreference.find( '.MuiListItemText-primary' ).textContent
+        )
@@ -60,2 +65,2 @@ test( 'can toggle back switch button from on state', async ( t ) => {
-        .notOk()
-} )
+        .notOk();
+} );
@@ -64 +69 @@ test( 'Go back from Settings page to Home', async ( t ) => {
-    navigateToSettingsPage( t )
+    navigateToSettingsPage( t );
@@ -71,2 +76,2 @@ test( 'Go back from Settings page to Home', async ( t ) => {
-        .contains( '#/' )
-} )
+        .contains( '#/' );
+} );
